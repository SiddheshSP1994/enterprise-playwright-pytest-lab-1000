[pytest]
addopts = --strict-markers -ra
markers =
    regression: Regression suite encompassing all generated tests
    smoke: Lightweight suite for critical flows
    api: API and contract tests
testpaths = tests
python_files = test_*.py